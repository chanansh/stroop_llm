# Stroop LLM

A project for running Stroop effect experiments with LLMs.

## Installation

```bash
poetry install
```

## Usage

Run the experiment:

```bash
poetry run python src/test_experiment.py
``` 